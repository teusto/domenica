import styles from "./styles/title.module.scss"

const Title = () => {
    return (
        <div className={styles.title_wrapper}>
            <h1>Projects</h1>
        </div>
    )
}

export default Title